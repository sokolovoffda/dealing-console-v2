# LEARN-001 — Vite scaffold

## Исходная задача

Создать с нуля web-приложение Vue 3 + TypeScript на Vite в корне `dealing-console-v2`. Это первый шаг фазы F0 (LEARN-000): только каркас tooling, без FSD-слоёв, Pinia, Router и WUI.

## Уточнения пользователя

- Scaffold: чистый Vite + руками дальше FSD (LEARN-002).
- Код в основном пишет разработчик; агент — план и ревью (код — по просьбе).
- i18n не подключаем; тексты на русском.
- `@wui/*` — в LEARN-004, здесь не ставить.

## Ограничения

- Не копировать целиком `.reference/`.
- Не добавлять Electron, PWA, i18n, Vitest.
- Не раскладывать FSD-папки в этом LEARN (это LEARN-002).
- Не коммитить секреты; корневой `.npmrc` уже есть — не трогать токены в чате/доках.
- Один шаг плана ≈ один коммит.

## Связанные материалы

- LEARN-000: `.ai/tasks/LEARN-000-app-roadmap/implementation_plan_1.md` (фаза F0)
- Ориентир версий (не копировать лишнее): `.reference/package.json` — `vue` ^3.4, `vite` ^5.4, `typescript` ^5.5, `@vitejs/plugin-vue` ^5.1
- Порт dev в референсе: `5558` — в учебном можно взять тот же или `5173`; зафиксируем в плане
