# Shared defaults for all Vite modes. Production web (`npm run build` / deb)
# must stay same-origin: nginx on the stand proxies /api and /ws.
# Dev stand hosts belong only in .env.development / .env.stage.

VITE_ELECTRON=false
