# Production / rtu-turret-console-dealing deb.
# Empty additional API URL so the browser uses same-origin; nginx proxies /api and /ws.
VITE_ADDITIONAL_API_URL=
VITE_API_DEV_SERVER=
VITE_SERVER_ELECTRON=
