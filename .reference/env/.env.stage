VITE_ENVIRONMENT_NAME=Stage

# Stage web (`npm run build:stage` / build-deb-stage) must stay same-origin:
# nginx on the stand proxies /api and /ws. Do not bake a remote additional API host.
VITE_ADDITIONAL_API_URL=
VITE_API_DEV_SERVER=
VITE_SERVER_ELECTRON=
