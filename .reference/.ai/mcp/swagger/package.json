{
  "name": "org-swagger-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local STDIO MCP server for organization Swagger/OpenAPI access.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
